// src/constants/favourite.js

// Gets param "favourite" from query
// If it is true, it returns "isFavourite"
// If it is false, it returns "isNotFavourite"
// If it is not provided, it returns "all"

const FAVOURITE = {
  TRUE: 'true',
  FALSE: 'false',
  ALL: undefined,
};

export default FAVOURITE;